# Boteco das IAs

Boletim semanal de notícias de tecnologia (Java, Spring Boot, Spring AI, Technology),
onde cada notícia é comentada por um "boteco" de IAs. O público são desenvolvedores
e estudantes de TI, e todo o texto (menos termos técnicos) é em português do Brasil.

Cada edição é gerada localmente, a custo zero: Spring Boot + Spring AI, modelos locais
no [Ollama](https://ollama.com), o cliente local do Claude Code e imagens anime pelo
[ComfyUI](https://github.com/comfyanonymous/ComfyUI).

## Os comentaristas

Para cada notícia, quatro opiniões em formato de conversa:

| Reviewer | Modelo | Como |
|----------|--------|------|
| Fernando Boaglio | — | digitado por você (`scripts/input-opinion.sh`) |
| Phi-4 mini | `phi4-mini` (Microsoft) | Ollama |
| Llama 3.2 | `llama3.2` (Meta) | Ollama |
| Claude | Claude Code | CLI local (`claude -p`) |

## Pipeline

Cada estágio lê/grava o mesmo `releases/<data>/magazine.json`:

```bash
mvn package
java -jar target/boteco-das-ias-*.jar gather      # 1. melhores notícias por assunto (sem repetir)
java -jar target/boteco-das-ias-*.jar translate   # 2. traduz título/resumo para pt-BR
java -jar target/boteco-das-ias-*.jar collect      # 3. opiniões dos modelos
scripts/input-opinion.sh                           #    sua opinião (opcional, interativo)
java -jar target/boteco-das-ias-*.jar illustrate   # 4. imagem anime por notícia (sem texto)
java -jar target/boteco-das-ias-*.jar render       # 5. magazine.html + cards do LinkedIn
```

As etapas também podem ser encadeadas: `java -jar ... gather translate collect illustrate render`.

## Saída

- **HTML**: `releases/<data>/magazine.html`, publicado no GitHub Pages (veja as edições abaixo).
- **LinkedIn**: uma imagem por notícia + sua conversa, geradas por
  `scripts/linkedin-images.sh [data]` em `releases/<data>/linkedin/`, para subir
  manualmente em um artigo do LinkedIn.

Para atualizar a página inicial e a lista de edições: `scripts/publish.sh`, depois `commit` + `push`.

## Ambiente local

```bash
docker compose up                                  # ComfyUI em http://localhost:8188
# baixe um checkpoint SD 1.5 em comfyui/models/checkpoints/v1-5-pruned-emaonly.safetensors
ollama pull phi4-mini && ollama pull llama3.2      # modelos dos reviewers
```

Detalhes de configuração em `src/main/resources/application.yml`.

## Edições

<!-- EDITIONS:START (generated by scripts/publish.sh) -->
- **2026-06-21** — [Boteco das IAs — 2026-06-21](https://boaglio.github.io/boteco-das-ias/releases/2026-06-21/magazine.html)
<!-- EDITIONS:END -->
